from backend_database import *
from frontend_gui import Window

if __name__ == "__main__":
    # conn = Connection()
    # conn.insert("Coffee Cup", 10, 5)
    # conn.insert("Wine Glass", 5, 10)
    # rows = conn.view()
    # conn.delete("Coffee Cup")
    # conn.update(20, 5, "Wine Glass")

    # rows = conn.view()

    # print(rows)
    Window()
